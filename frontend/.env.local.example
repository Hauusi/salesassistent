# Copy to .env.local
#
# Inlined into the client bundle at build time (that is what NEXT_PUBLIC_
# means), so changing it requires a rebuild, not just a restart.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
